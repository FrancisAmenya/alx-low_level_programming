#ifndef MAIN_H
#define MAIN_H

void _puts_recursion(char *pr);

int _putchar(char c);

void _print_rev_recursion(char *prr);

int _strlen_recursion(char *sr);

int factorial(int f);

int _pow_recursion(int x, int y);

int _sqrt_recursion(int sr);

int is_prime_number(int ipm);

int is_palindrome(char *ip);

int wildcmp(char *s1, char *s2);

#endif
